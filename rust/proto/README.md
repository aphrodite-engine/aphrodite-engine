# gRPC protocol

This directory contains Sonar's gRPC schema. Rust builds use these local files.
The Buf configuration supports local schema linting. Sonar does not configure
automatic publication to a Buf registry.
